#!/usr/bin/env python3

from src.views.base.tui import TUI
from src.views.panels.main import MainPanel

if __name__ == "__main__":
    main_view = MainPanel()
    tui = TUI(main_view.root, footer=main_view.main_helper_text)
    try:
        tui()
    finally:
        tui.loop.screen.stop()
